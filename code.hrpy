fn tetorysta(sus1:int, sus2: &mut str) -> bool {
  if sus1 < len(sus2) {
    trim(sus2, sus1);
    return true;
  }

  let sus :int some random bullshit;

  mogus = 5 + );
  huh +=;
  let epic :false = 5;

  return false;
}
